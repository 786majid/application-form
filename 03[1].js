// Change Text

// textContent and innerText           (with the help of text content  we can change the text)
const mainHeading = document.getElementById("main-heading");  //it is used to select the content
console.log(mainHeading.textContent);   
mainHeading.textContent="Everything is possible if u try";  //it is used to change the content
console.log(mainHeading.textContent);